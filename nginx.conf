server {
    listen 80;
    listen [::]:80;
    server_name corptlux.test www.corptlux.test corptlux.com www.corptlux.com localhost 127.0.0.1;

    # Certbot challenge validation
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Proxy requests to Next.js container
    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── Configuración SSL (Activar al emitir certificados en el servidor de producción) ──
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name corptlux.test www.corptlux.test corptlux.com www.corptlux.com;
# 
#     ssl_certificate /etc/letsencrypt/live/corptlux.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/corptlux.com/privkey.pem;
# 
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
# 
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
# 
#     location / {
#         proxy_pass http://web:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
